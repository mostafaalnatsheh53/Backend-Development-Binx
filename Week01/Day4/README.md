# Day 4

Today I learned and practiced the following concepts:

- Worked with different C# collections (`List`, `Dictionary`, and `HashSet`).
- Used LINQ to filter, project, and aggregate data using `Where()`, `Select()`, and `Count()`.
- Learned the basics of asynchronous programming with `async` and `await`.
- Created an asynchronous method using `Task.Delay()` and awaited it from `Main()`.
- Practiced exception handling using `try`, `catch`, and `FormatException`.
- Completed a hands-on lab by creating a `User` model, writing LINQ queries, simulating an asynchronous operation, and safely handling user input.